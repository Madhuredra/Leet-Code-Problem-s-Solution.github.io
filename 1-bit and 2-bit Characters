c++ slution :
int len = bits.size();
        int pos = 0;
        bool c = true;
        for (int i = 0; i < len; i++) {
            c = true;
            if (bits[i] == 1) {
                i++;
                c = false;
            }
        }
        return c;
